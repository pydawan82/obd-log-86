[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "log86"
version = "0.0.1"
dependencies = [
    "obd ~=0.7"
]

[project.scripts]
log86 = "log86:main"
